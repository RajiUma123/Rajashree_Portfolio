import { defineConfig } from 'vite';

export default defineConfig({
  base: '/Rajashree_Portfolio/', // Set the base path for GitHub Pages
  build: {
    outDir: 'dist', // Ensure the build output goes to the 'dist' folder
  },
});
